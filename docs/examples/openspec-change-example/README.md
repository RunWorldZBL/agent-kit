# OpenSpec Change Example

This directory shows the intended shape for an OpenSpec change without making it active.

Copy this example to `openspec/changes/<change-name>/` when starting a real non-trivial feature.
