# Design: Add Items Feature

## Data Contract

Use `shared/api-contracts/src/items.ts` as the contract boundary.

## Backend

Create route handlers under the appropriate backend tier.

## Frontend

Consume the shared contract through the frontend API client.
